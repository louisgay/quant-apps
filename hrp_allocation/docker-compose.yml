services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1

  tests:
    build: .
    command: pytest tests/ -v
    profiles:
      - test
