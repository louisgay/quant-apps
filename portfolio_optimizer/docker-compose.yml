services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    restart: unless-stopped

  tests:
    build: .
    entrypoint: ["pytest", "tests/", "-v"]
    profiles:
      - test
